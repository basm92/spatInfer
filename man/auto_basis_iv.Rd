% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_basis_iv.R
\name{auto_basis_iv}
\alias{auto_basis_iv}
\title{Automated spatial IV regression with optimal basis and cluster selection}
\usage{
auto_basis_iv(
  fm,
  df,
  max_splines = 8,
  nSim = 1000,
  weights = FALSE,
  max_clus = 6,
  max_pc = 6,
  Parallel = TRUE,
  exact_cholesky = TRUE,
  k_medoids = TRUE,
  jitter_coords = TRUE,
  cov = "BCH",
  verbose = TRUE
)
}
\arguments{
\item{fm}{The IV formula in the form \code{y ~ exog_controls | endog ~ instruments}.
See \code{\link[=basis_regression_iv]{basis_regression_iv()}} for details.}

\item{df}{The dataset. Must contain columns \code{X} (longitude) and \code{Y}
(latitude) with no missing values.}

\item{max_splines}{Maximum tensor dimension to examine (3 to 12). Defaults
to 8.}

\item{nSim}{Number of placebo simulations for cluster selection. Defaults to
1000; use a smaller value (e.g. 100) for exploratory runs.}

\item{weights}{Set \code{weights=TRUE} if the regression is weighted. The
weighting variable must be named \code{wts} in \code{df} for
\code{\link[=basis_regression_iv]{basis_regression_iv()}} and \code{weights} in \code{df} for \code{\link[=placebo]{placebo()}}.}

\item{max_clus}{Maximum number of clusters to examine in the placebo test.
Defaults to 6.}

\item{max_pc}{Maximum number of principal components to consider when
searching for the BIC-optimal basis. Defaults to 6. Increase if you
suspect the spatial trend requires more components; decrease to keep the
regression parsimonious.}

\item{Parallel}{Run simulations in parallel. Set to \code{FALSE} if there are
memory problems.}

\item{exact_cholesky}{For very large datasets, set to \code{FALSE} to use the
BRISC Cholesky approximation.}

\item{k_medoids}{For large datasets, set to \code{FALSE} to use CLARA instead of
PAM for medoid computation.}

\item{jitter_coords}{If some sites share identical coordinates, jitter by
Gaussian noise (sd = 0.01) so the Moran test can be computed.}

\item{cov}{Covariance estimator passed to \code{\link[=basis_regression_iv]{basis_regression_iv()}}. Defaults
to \code{"BCH"}.}

\item{verbose}{If \code{TRUE} (default), prints the chosen splines, PC count, and
cluster number to the console.}
}
\value{
A named list with elements:
\describe{
\item{\code{result}}{A \code{fixest} object from \code{\link[=basis_regression_iv]{basis_regression_iv()}}. Can be
passed directly to \code{modelsummary::modelsummary()} and
\code{fixest::fitstat()}.}
\item{\code{splines}}{Chosen tensor spline dimension.}
\item{\code{pc_num}}{Chosen number of principal components.}
\item{\code{clusters}}{Chosen number of BCH clusters.}
\item{\code{placebo}}{Full list returned by \code{\link[=placebo]{placebo()}}, containing \code{Results}
and \code{Spatial_Params}.}
}
}
\description{
\code{auto_basis_iv()} runs the full three-step spatial IV pipeline automatically:
\enumerate{
\item \strong{Optimal basis} — sweeps tensor spline dimensions from 3×3 to
\code{max_splines}×\code{max_splines}, extracts principal components at each
dimension, and chooses the (spline, PC count) combination that minimises
BIC for the outcome variable. This replicates the logic of
\code{\link[=optimal_basis]{optimal_basis()}} but returns values instead of a plot.
\item \strong{Optimal clusters} — runs \code{\link[=placebo]{placebo()}} using the endogenous variable as
the "treatment" (so that its spatial structure drives the noise
simulations) and automatically selects the cluster count whose placebo
rejection rate at the 5 \% level is closest to 0.05.
\item \strong{IV regression} — calls \code{\link[=basis_regression_iv]{basis_regression_iv()}} with the chosen
parameters and returns its output together with the selected values and
the full placebo diagnostics.
}
}
\examples{
library(spatInfer)
data(opportunity)
set.seed(123)
opp <- opportunity |> dplyr::slice_sample(n = 150)

res <- auto_basis_iv(
  mobility ~ gini | single_mothers ~ dropout_rate,
  opp,
  max_splines = 6, nSim = 100, Parallel = FALSE
)

# Chosen parameters
res$splines; res$pc_num; res$clusters

# First-stage F-statistic
fixest::fitstat(res$result, "ivf")

# Regression table
modelsummary::modelsummary(list(`IV-BCH` = res$result),
  statistic = c("conf.int", "p = {p.value}"),
  coef_omit = "Intercept|PC", fmt = 2)
}
