% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moran_test.R
\name{moran_test}
\alias{moran_test}
\title{Moran's I test on the residuals of a fixest model}
\usage{
moran_test(model, df = NULL, near_neigh = 5)
}
\arguments{
\item{model}{A \code{fixest} object (from \code{\link[fixest:feols]{fixest::feols()}}, \code{\link[=basis_regression]{basis_regression()}},
or \code{\link[=basis_regression_iv]{basis_regression_iv()}}).}

\item{df}{A data frame with columns \code{X} (longitude) and \code{Y} (latitude), one
row per observation used in \code{model}. If \code{NULL} (default), the function
looks for data saved in \code{model$data}; this is automatically available for
objects returned by \code{\link[=basis_regression]{basis_regression()}} and \code{\link[=basis_regression_iv]{basis_regression_iv()}} because
they call \code{feols(..., data.save = TRUE)} internally.}

\item{near_neigh}{Number of nearest neighbours used to build the spatial
weights matrix. Default \code{5}.}
}
\value{
An object of class \code{moran_test} with components:
\describe{
\item{\code{statistic}}{The observed Moran's I statistic.}
\item{\code{p.value}}{One-sided p-value (H1: positive spatial autocorrelation).}
}
}
\description{
Computes Moran's I for spatial autocorrelation in the residuals of a
\code{\link[fixest:feols]{fixest::feols()}} object, including those returned by \code{\link[=basis_regression]{basis_regression()}}
and \code{\link[=basis_regression_iv]{basis_regression_iv()}}. Use this as a post-estimation diagnostic: after
adding a spatial basis the residuals should show little remaining
autocorrelation.
}
\details{
The weights matrix is built from the \code{k} nearest neighbours of each
observation (row-standardised), matching the approach used internally by
\code{\link[=placebo]{placebo()}} and \code{\link[=synth]{synth()}}. Duplicate coordinates are jittered by a small
random amount before neighbour search.

For \code{modelsummary} integration, \code{moran_i} and \code{moran_p} are added
automatically to the goodness-of-fit section when \code{spatInfer} is loaded and
the model has spatial data (i.e. \code{model$data} contains \code{X} and \code{Y}). Control
the display with \code{gof_map}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{modelsummary::modelsummary(
  list(CK = ck),
  gof_map = list(
    list(raw = "nobs",     clean = "N",       fmt = 0),
    list(raw = "r.squared", clean = "R\\u00b2", fmt = 2),
    list(raw = "moran_i",  clean = "Moran I", fmt = 3),
    list(raw = "moran_p",  clean = "Moran p", fmt = 3)
  )
)
}\if{html}{\out{</div>}}

For a plain \code{\link[fixest:feols]{fixest::feols()}} call (without \code{data.save = TRUE}), supply
coordinates explicitly and use the \code{gof_function} argument:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{modelsummary::modelsummary(
  list(OLS = mod),
  gof_function = function(m, ...) \{
    mt <- moran_test(m, df = mydata)
    data.frame(moran_i = mt$statistic, moran_p = mt$p.value)
  \}
)
}\if{html}{\out{</div>}}
}
\examples{
library(spatInfer)
data(opportunity)
set.seed(123)
opportunity <- opportunity |> dplyr::slice_sample(n = 250)

ck <- basis_regression(mobility ~ single_mothers + gini, opportunity,
                       splines = 4, pc_num = 3, clusters = 5)

# Standalone test (coordinates inferred from model$data)
moran_test(ck)

# Passing coordinates explicitly
moran_test(ck, df = opportunity)

# In modelsummary — moran_i / moran_p appear automatically via glance_custom
modelsummary::modelsummary(
  list(CK = ck),
  statistic  = c("conf.int", "p = {p.value}"),
  coef_omit  = "Intercept|PC",
  gof_map    = list(
    list(raw = "nobs",      clean = "N",       fmt = 0),
    list(raw = "r.squared", clean = "R2",       fmt = 2),
    list(raw = "moran_i",   clean = "Moran I",  fmt = 3),
    list(raw = "moran_p",   clean = "Moran p",  fmt = 3)
  )
)
}
