% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basis_regression_iv.R
\name{basis_regression_iv}
\alias{basis_regression_iv}
\title{Carry out spatial basis IV (2SLS) regressions with BCH standard errors.}
\usage{
basis_regression_iv(
  fm,
  df,
  splines,
  pc_num,
  clusters,
  weights = FALSE,
  cov = "BCH"
)
}
\arguments{
\item{fm}{The IV formula in the form \code{y ~ exog_controls | endog ~ instruments}.
\code{y} is the outcome, \code{exog_controls} are additional exogenous regressors
(use \code{1} if none), \code{endog} is the endogenous variable of interest, and
\code{instruments} are the excluded instruments. Multiple instruments or controls
are separated by \code{+} in the usual way, e.g.
\code{y ~ ctrl1 + ctrl2 | endog ~ z1 + z2}.}

\item{df}{The dataset. Longitude and latitude must be named \code{X} and \code{Y} and
have no missing values.}

\item{splines}{The dimension of the linear tensor spline basis, from
\code{\link[=optimal_basis]{optimal_basis()}}.}

\item{pc_num}{The number of principal components to include, from
\code{\link[=optimal_basis]{optimal_basis()}}.}

\item{clusters}{Number of k-medoids clusters for BCH standard errors, chosen
using the placebo test from \code{\link[=placebo]{placebo()}}.}

\item{weights}{Set \code{weights=TRUE} if the regression is weighted. The
weighting variable in the dataset must be named \code{wts}.}

\item{cov}{Defaults to \code{"BCH"} for large-cluster bias-corrected
heteroskedasticity-consistent standard errors. Any other value gives plain
heteroskedasticity-consistent standard errors.}
}
\value{
A \code{fixest} object (from \code{\link[fixest:feols]{fixest::feols()}}) that can be printed and
exported using the \code{modelsummary} package. The first-stage F-statistic is
accessible via \code{fixest::fitstat(result, "ivf")}. Because t-statistics from
large-cluster regressions have low degrees of freedom, reporting confidence
intervals and p-values (rather than standard errors) is recommended in
\code{modelsummary} output.
}
\description{
\code{basis_regression_iv()} runs two-stage least squares (2SLS) with a spatial
basis added as exogenous controls, and large-cluster BCH or heteroskedasticity-
consistent standard errors. It mirrors \code{\link[=basis_regression]{basis_regression()}} in interface and
output but accepts an IV formula specifying an endogenous variable and one or
more instruments.

The spatial basis principal components are added to the exogenous part of the
first-stage and reduced-form regressions, de-trending the data and reducing
residual spatial correlation before inference.
}
\examples{
library(spatInfer)
data(opportunity)
set.seed(123)
opportunity <- opportunity |> dplyr::slice_sample(n=250)
# Instrument single_mothers with dropout_rate, controlling for gini.
# Use splines/PCs from optimal_basis and clusters from placebo.
ck_iv <- basis_regression_iv(
  mobility ~ gini | single_mothers ~ dropout_rate,
  opportunity,
  splines = 4, pc_num = 3, clusters = 4
)

# First-stage F-statistic
fixest::fitstat(ck_iv, "ivf")

# Regression table (omit basis PCs and intercept)
modelsummary::modelsummary(list(`IV-BCH` = ck_iv),
  statistic = c("conf.int", "p = {p.value}"),
  coef_omit = "Intercept|PC",
  gof_map = c("nobs", "r.squared"), fmt = 2)
}
